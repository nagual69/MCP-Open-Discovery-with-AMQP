{
  "name": "@mcp-open-discovery/net-utils-plugin",
  "private": true,
  "version": "1.0.0",
  "description": "Typed source package for the net-utils MCP plugin",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:strict": "tsc -p tsconfig.strict.json --noEmit"
  }
}